import { StyleSheet } from 'react-native'

import { colors } from '@styles/theme'

export const styles = StyleSheet.create({
  container: {
    paddingTop: 48,
    paddingBottom: 32 + 52 / 2,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: colors.gray_700,
  },
})
